/**
 * Interface for getting the string representation of an object
 */
public interface Printable {

	// return a comma separated string representation of the object, such that it
	// can be printed (to a file)
	String getFileData();
}
